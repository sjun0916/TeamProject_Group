<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="report">

	<select id="select" parameterType="UserVO"
		resultType="com.group.approve.vo.ReportVo">
		select REPORT_NO,REG_TITLE,REG_DATE,REG_STATE,REG_REASON from REPORT where REG_ID=#{employeeNo}
	</select>
	
	
	<select id="select2" parameterType="int"
		resultType="com.group.approve.vo.ReportVo">
		SELECT REPORT_NO,REG_TITLE,REG_DATE,REG_STATE,REG_REASON from REPORT
		and label_num =#{label_num}
	</select>
	
	
	<select id="selectOne" parameterType="int"
		resultType="com.group.approve.vo.ReportVo">
		select * from REPORT where REPORT_NO=#{employeeNo}
	</select>
	
	
	<select id="check" parameterType="UserVO"
		resultType="com.group.approve.vo.ReportVo">
		SELECT REPORT_NO,REG_TITLE,REG_DATE,REG_STATE,REG_REASON from REPORT
		WHERE REPORT_NO IN
		(SELECT REPORT_NO from REPORT
		WHERE REPORT_NO IN(
		select REPORT_NO from REPORT
		WHERE REG_MANAGER1 =#{employeeNo}
    	and REG_UPDATE1 IS NULL
		)
		OR REPORT_NO IN(
		select REPORT_NO from REPORT
		WHERE REG_MANAGER2 =#{employeeNo}
		and REG_UPDATE1 is not null
    	and REG_UPDATE2 is null
		)
		or REPORT_NO IN(
		select REPORT_NO from REPORT
		where REG_MANAGER3 =#{employeeNo}
		and REG_UPDATE2 is not null
    	and REG_UPDATE2 is null
		)
		or REPORT_NO IN(
		select REPORT_NO from REPORT
		where REG_MANAGER4 =#{employeeNo}
		and REG_UPDATE3 is not null
    	and REG_UPDATE4 is null
		)or REPORT_NO IN(
		select REPORT_NO from REPORT
		where REG_MANAGER5 =#{emplyeeNo}
		and REG_UPDATE4 is not null
    	and REG_UPDATE5 is null
		)
  )
  and REG_STATE !=0
  and REG_STATE !=3
	</select>


	<insert id="insert" parameterType="com.group.approve.vo.ReportVo">
		insert into REPORT (
		REPORT_NO,
		REG_ID,
		REG_DEP,
		REG_MANAGER1,
		REG_MANAGER2,
		REG_MANAGER3,
		REG_MANAGER4,
		REG_MANAGER5,
		REG_TITLE,
		REG_CONT,
		REG_STATE,
		REG_DATE,
		REG_TYPE,
		REG_COMMENT
		) VALUES (
		REPORT_SEQ.NEXTVAL,
		#{reg_id},
		#{reg_dep},
		#{reg_manager1},
		#{reg_manager2,jdbcType=INTEGER},
		#{reg_manager3,jdbcType=INTEGER},
		#{reg_manager4,jdbcType=INTEGER},
		#{reg_manager5,jdbcType=INTEGER},
		#{reg_title},
		#{reg_cont},
		#{reg_state},
		SYSDATE,
		#{reg_type,jdbcType=VARCHAR},
		#{reg_comment,jdbcType=VARCHAR}
		)
	</insert>
	
	
	<update id="updateReport" parameterType="com.group.approve.vo.ReportVo">
		UPDATE REPORT
		<choose>
		<when test="reg_update1 == null">
		set REG_UPDATE1 =SYSDATE
		<choose>
		<when test="reg_manager2 != null">
		,REG_STATE=2
		</when>
		<when test="reg_manager2 == null">
		,REG_STATE=3
		</when>
		</choose>
		</when>
		<when test="reg_update2 == null">
		set REG_UPDATE2 =SYSDATE
		<choose>
		<when test="reg_manager3 != null">
		,REG_STATE=2
		</when>
		<when test="reg_manager3 == null">
		,REG_STATE=3
		</when>
		</choose>
		</when>
		<when test="reg_update3 == null">
		set REG_UPDATE3 =SYSDATE
		<choose>
		<when test="reg_manager4 != null">
		,REG_STATE=2
		</when>
		<when test="reg_manager4 == null">
		,REG_STATE=3
		</when>
		</choose>
		</when>
		<when test="reg_update4 == null">
		set REG_UPDATE4 =SYSDATE
		<choose>
		<when test="reg_manager5 != null">
		,REG_STATE=2
		</when>
		<when test="reg_manager5 == null">
		,REG_STATE=3
		</when>
		</choose>
		</when>
		<when test="reg_update5 == null">
		set REG_UPDATE5 =SYSDATE
		,REG_STATE=3
		</when>
		</choose>
		where REPORT_NO=#{report_no}
	</update>
	
	
	<update id="backReport" parameterType="com.group.approve.vo.ReportVo">
		update REPORT set
		REG_REASON=#{reg_reason,jdbcType=VARCHAR},REG_STATE=0
		where REPORT_NO=#{report_no,jdbcType=VARCHAR}
	</update>
	
	
	<select id="surch" parameterType="hashmap" resultType="com.group.approve.vo.ReportVo">
			select * from
			(select * from
			(select REG_DATE
						  ,REG_ID
						  ,REG_TITLE
						  ,REPORT_NO
						  ,REG_STATE
			from REPORT
			where REG_ID != #{sessionid,jdbcType=VARCHAR}) 
			where
			     REPORT_NO like '%'||#{keyword,jdbcType=VARCHAR}||'%'
					or REG_ID like '%'||#{keyword,jdbcType=VARCHAR}||'%'
					or REG_TITLE like '%'||#{keyword,jdbcType=VARCHAR}||'%')
			    where REG_STATE =0 or REG_STATE =3
	</select>
	
	
	<select id="waitCont" parameterType="String" resultType="int">
		select count(*) 
		from REPORT
		where REG_ID=#{id,jdbcType=VARCHAR} 
		and REPORT_NO
		in(select REPORT_NO
		from REPORT
		where REG_STATE = '1'
		or REG_STATE='2')
	</select>
	
	
	<select id="endCont" parameterType="String" resultType="int">
		select count(*) 
		from REPORT
		where REG_ID=#{id,jdbcType=VARCHAR} 
		and REPORT_NO
		in(select REPORT_NO
		from REPORT 
		where REG_STATE = '3')
	</select>
	
	
	<select id="backCont" parameterType="String" resultType="int">
		select count(*) 
		from REPORT
		where REG_ID=#{id,jdbcType=VARCHAR} 
		and REPORT_NO 
		in(select REPORT_NO
		from REPORT 
		where REG_STATE = '0')
	</select>
	

</mapper>